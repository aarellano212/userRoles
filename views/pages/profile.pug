extends ../layout

block content
    div.content
        h1 Your role is #{user.role.name}

        h3 You can
        ul
            each permissions in user.role.permissions
                li=permission.name